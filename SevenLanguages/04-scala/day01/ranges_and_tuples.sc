val range = 0 until 10

range.start

range.end

range.step

(0 to 10) by 5

(0 to 10) by 6

(0 until 10 by 5)

val range1 = (10 until 0) by -1

var range2 = (10 until 0)

val range3 = (0 until 10)

val range4 = 'a' to 'e'

// tuples

val person = ("Elvis", "Presley")

person._1

person._2

//person._3

val (x, y) = (1, 2)

val (a, b) = (1, 2, 3)

